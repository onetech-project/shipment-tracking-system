{
  "name": "shipment-tracker",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/backend",
    "apps/frontend",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm:dev:backend\" \"pnpm:dev:frontend\"",
    "dev:backend": "pnpm --filter apps/backend start:dev",
    "dev:backend:debug": "pnpm --filter apps/backend start:debug",
    "dev:frontend": "pnpm --filter apps/frontend dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "test:e2e": "pnpm -r run test:e2e",
    "lint": "pnpm -r run lint",
    "migration:run": "pnpm --filter apps/backend migration:run",
    "migration:revert": "pnpm --filter apps/backend migration:revert",
    "migration:generate": "pnpm --filter apps/backend migration:generate",
    "migration:show": "pnpm --filter apps/backend migration:show",
    "schema:drop": "pnpm --filter apps/backend schema:drop"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
